[functions.experiment-config]
# Anonymous installations fetch only a resolved treatment. The function reads tables
# with the server-side service role; clients never receive database credentials.
verify_jwt = false

[functions.analytics-events]
# Research events are pseudonymous installation/session events and are accepted through
# this validating ingestion endpoint. Database tables themselves remain server-side only.
verify_jwt = false
